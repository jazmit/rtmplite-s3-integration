More rtmplite details at
http://code.google.com/p/rtmplite and http://code.google.com/p/siprtmp

You must install boto which is Python interface to Amazon Web
Services.
More boto at https://github.com/boto/boto

Then you must config the S3.ini with S3 identity.

After all type `python rtmp.py -h` for more details to run trmplite.

This application will upload the recorde file to S3 automatically.

If you wanted to recorde stream with rtmpt procotol, type `python
rtmpt.py -h` for more details.

Logging file stored in `/var/log/rtmplite.log`.
